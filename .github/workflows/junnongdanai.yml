name: junnongdanai
on:
  workflow_dispatch:

  schedule:
    - cron: '50 5,11,17,23 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: junnongdanai
        env:
          tpli4207: ${{ secrets.tpli4207 }}
        run: |
          printf '%s' "$tpli4207" | jq -r .vqjg64963 | /bin/bash
          printf '%s' "$pqd504" | jq -r .yjle7 | /bin/bash
          echo $msu7649 | jq -r .uovke046 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: teTLfTRi
          path: "*.zip"
          retention-days: 1
